  <div class="review-user-box">
  <div class="user-name-box">
    <span><%= review.user.name %> さんの口コミ</span>
  </div>
  <div class="review-description-box">
    <p>
      <%= review.content %>
    </p>
    <% if current_user == review.user %>
      <%= link_to "口コミを削除する", menu_review_path(review, review.id),
          method: :delete, class: "review-delete-btn", method: :delete, data: {confirm: "本当に削除しますか？"} %>
    <% end %>
  </div>
</div>